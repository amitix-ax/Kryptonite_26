# Optional heavier/less-commonly-needed dependencies (pyproject.toml's
# [project.optional-dependencies].optional). Install with:
#   pip install -r requirements.txt -r requirements-optional.txt
netCDF4>=1.6
h5py>=3.10
scikit-image>=0.22
numba>=0.58
torch>=2.1
